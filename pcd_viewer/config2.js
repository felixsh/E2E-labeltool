// config2.js
window.PCD_CONFIG = {
  // Optional default PCD file path (relative or absolute URL)
  defaultPCD: "pointclouds/0000000505.pcd",

  // Rendering defaults
  pointSize: 0.08,            // ~8 cm per point
  colorMode: "height", // "height" | "intensity" | "distance" | "solid"
  maxPoints: 200000,
  exposure: 1.0
};
